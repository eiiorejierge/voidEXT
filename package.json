{
  "name": "nebula",
  "version": "1.0.0",
  "private": true,
  "description": "Nebula — bookmarklet-driven link rotator with account auth, backed by a Vercel site.",
  "scripts": {
    "dev": "node dev-server.js",
    "build:bookmarklet": "node build-bookmarklet.js"
  },
  "engines": {
    "node": ">=18"
  }
}
