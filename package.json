{
  "name": "voidext",
  "version": "2.9.9",
  "private": true,
  "description": "Scale XT - private bookmarklet-driven link rotation with account authentication.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:bookmarklet": "node build-bookmarklet.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.50.0"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
